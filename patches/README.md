# Build Patches

This directory stores small, pinned-source patches applied during image builds.

Patch subdirectories should name the upstream source they modify and keep patches narrow, reproducible, and tied to an exact pinned SHA in `Dockerfile`.
